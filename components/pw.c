#include "pw.h"
#include <stdio.h>

float pointwise_conv(float *input, float *output, float *kernel, int input_h,
                    int input_w, int kernel_h, int kernel_w) {
                        
}